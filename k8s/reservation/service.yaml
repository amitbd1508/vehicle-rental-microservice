apiVersion: v1
kind: Service
metadata:
  name: reservation-svc
  annotations:
    cloud.google.com/neg: '{"ingress": true}'
spec:
  selector:
    app: reservation
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: LoadBalancer
