/*
import {ComponentFixture, TestBed} from '@angular/core/testing';

import {ProductListItemDetailsComponent} from './product-list-item-details.component';

describe('ProductListItemDetailsComponent', () => {
  let component: ProductListItemDetailsComponent;
  let fixture: ComponentFixture<ProductListItemDetailsComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ProductListItemDetailsComponent]
    })
      .compileComponents();
  });

  beforeEach(() => {
    fixture = TestBed.createComponent(ProductListItemDetailsComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
*/
